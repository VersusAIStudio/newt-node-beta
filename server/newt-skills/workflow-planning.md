Prefer the user's established way of working over inventing a new graph. First inspect the existing project and the current saved preset library. Read a promising preset's details before selecting it; names alone do not establish suitability. Use complementary presets for different branches when appropriate, such as headshot, location and prop preparation. Do not force a full template onto a small change to an existing node.

Record the workflow basis in the plan: presets with their IDs, existing work, or custom with a concrete reason no preset fits. Preserve a preset's models, style, grade, camera settings and layout unless the brief explicitly calls for a change. Favorite models apply to fresh nodes, not preset copies. Identify which saved text is an example subject to replace and which text is reusable production guidance. Never carry an unrelated example character, location or output into the new story.

Use typed preset input bindings when available; connect suitable existing assets rather than duplicating them. If a preset has no input slots, inspect its text and asset nodes and adapt only the inserted copy through supported actions. Do not alter the saved library, source nodes, locked settings or approved work. If its locked content cannot be adapted, choose another workflow or explain the blocker.

Prefer free deterministic operations for creating, naming, grouping, wiring and inserting workflows. Use reasoning for creative decisions, not extra approval ceremonies. Inspect the actual inserted graph and IDs before subsequent actions. Reuse completed work. Organize this task's work on the shared grid. Preserve useful exploratory branches; only remove explicitly temporary preparation through the guarded cleanup tool after retaining the chosen asset independently. Do not repeat paid generations without a specific observed problem and sufficient permission and budget.

New tasks should consider the latest library. Active copies are independent of later preset updates. A changed preset must be inspected again before insertion; do not silently insert a different revision than the one reviewed.
